{% extends 'base.html.twig' %}

{% block body %}
    <h1>examen</h1>

    <table>
        <tbody>
            <tr>
                <th>Idexamen</th>
                <td>{{ examen.idexamen }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('examen_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('examen_edit', { 'idexamen': examen.idexamen }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
