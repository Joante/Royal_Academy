{% extends 'base.html.twig' %}

{% block body %}
    <div class="estilo-body">
        <h1>Pais creation</h1>ç

        <div class="formulario">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" class="bottom"/>
            {{ form_end(form) }}
        </div>
        <ul>
            <li>
                <a href="{{ path('pais_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
